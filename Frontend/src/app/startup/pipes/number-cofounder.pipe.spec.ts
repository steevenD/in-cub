import { NumberCofounderPipe } from './number-cofounder.pipe';

describe('NumberCofounderPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberCofounderPipe();
    expect(pipe).toBeTruthy();
  });
});
